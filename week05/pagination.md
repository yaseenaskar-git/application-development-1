# Pagination and filtering

### Which endpoints supports pagination
GET /users
GET /packages

### Pagination methods (page/limit or cursor)
page/limit

### Example query parameters
page=2&limit=10

GET /packages?page=2&limit=10